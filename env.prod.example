# 生产环境必填项说明：若你已在根目录有 `.env`，一般只需在里头补上 ALLOWED_HOSTS（及下面 CSRF/CORS）即可；
# 没有 `.env` 时可：cp env.prod.example .env.prod 再填。
# deploy 加载顺序：ENV_FILE → .env.prod → .env
#
# 必填项由 roamio/settings/prod.py 校验（见 require_env）。

ALLOWED_HOSTS=roamio.cn,www.roamio.cn

SECRET_KEY=replace-with-long-random-django-secret-key

# 云 MySQL/Tencent 不可用或已过期时设为 1：prod 使用项目根目录 db.sqlite3，可删掉或忽略下面 DB_*。
# ROAMIO_USE_SQLITE=1

DB_ENGINE=django.db.backends.mysql
DB_NAME=roamio
DB_USER=roamio_user
DB_PASSWORD=replace-with-db-password
DB_HOST=127.0.0.1
DB_PORT=3306

# 建议与 ALLOWED_HOSTS 的访问协议、域名一致（逗号分隔）
CSRF_TRUSTED_ORIGINS=https://roamio.cn,https://www.roamio.cn
CORS_ALLOWED_ORIGINS=https://roamio.cn,https://www.roamio.cn
