# Roamio frontend environment example.
# Leave VITE_API_BASE_URL empty to use same-origin API routing.
VITE_API_BASE_URL=
VITE_API_VERSION=v1

# Browser map SDKs are disabled. The UI uses copy/search links until map features reopen.
